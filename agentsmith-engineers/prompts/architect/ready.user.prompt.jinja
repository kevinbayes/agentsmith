Build a comprehensive backlog of tasks for the following project.

# IMPORTANT: The list of backlog tasks must be outputted in json format between a backlog tag in an array of task objects like the following example:
Example 1 for a new project with an empty directory for a ping api:
<backlog>
```
{
    "tasks": [
    {
        "task": "Create project",
        "description": "Create a rust api project using the approved technology stack."
    },
    {
        "task": "Create ping api",
        "description": "Create a ping api using an axum route with a GET method. The api should return 'pong'"
    },
    ]
}
```
</backlog>
{% include "common/project_details.prompt.jinja" %}