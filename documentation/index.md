# Agent Smith
Agent smith is an end to end operating system for AI. It is targeted at lowcode and low level users.

# Modules

## Agent 



## Runtime

The runtime is a deployment of agents that could be a simple proxy or deployed on your local machine. The orchestrator works with runtimes
to execute tasks.

## Orchestrator

The orchestrator accepts a request facilitates the execution of the request.

## Interface

The interface is the service specification for interacting with the system. It has interfaces (sync and async) used to communicate with the system.

## Web


